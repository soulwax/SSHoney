# SSHoney Configuration File
# This is an example configuration file for SSHoney
#
# Note: Currently, SSHoney does not parse configuration files.
# Configuration is done via command-line arguments.
# This file is provided as a reference for future implementation.
#
# Example command-line usage:
#   sshoney -p 2222 -d 10000 -l 64 -m 8192

# Port to listen on (default: 2222)
# Port 2222

# Delay between messages in milliseconds (default: 10000)
# Delay 10000

# Minimum delay when randomizing (default: 5000)
# MinDelay 5000

# Maximum delay when randomizing (default: 15000)
# MaxDelay 15000

# Maximum banner line length (default: 64)
# MaxLineLength 64

# Maximum concurrent clients (default: 8192)
# MaxClients 8192

# Bind to IPv4 only (-4 flag)
# BindFamily 4

# Bind to IPv6 only (-6 flag)
# BindFamily 6

# Enable random delay between min and max (default: true)
# RandomizeDelay true

# Enable TCP_NODELAY (default: true)
# TcpNoDelay true

